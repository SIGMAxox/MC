name: Extract World Folder

on:
  workflow_dispatch:  # تشغيل يدوي
  push:
    paths:
      - 'world.zip'  # يشتغل تلقائي لو رفعت world.zip جديد

# إضافة صلاحيات الكتابة
permissions:
  contents: write

jobs:
  extract-world:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Check if world.zip exists
        run: |
          if [ ! -f "world.zip" ]; then
            echo "Error: world.zip not found!"
            exit 1
          fi
          echo "world.zip found, extracting..."
      
      - name: Extract world.zip
        run: |
          # فك الضغط
          unzip -o world.zip
          
          # حذف الملف المضغوط (اختياري)
          # rm world.zip
          
          echo "Extraction completed!"
          ls -la
      
      - name: Commit and Push Changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🗺️ Extract world folder from world.zip"
          file_pattern: "world/*"
