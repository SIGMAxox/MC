name: Extract World Folder

on:
  workflow_dispatch:  # تشغيل يدوي
  push:
    paths:
      - 'world.zip'  # يشتغل تلقائي لو رفعت world.zip جديد

jobs:
  extract-world:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Check if world.zip exists
        run: |
          if [ ! -f "world.zip" ]; then
            echo "Error: world.zip not found!"
            exit 1
          fi
          echo "world.zip found, extracting..."
      
      - name: Extract world.zip
        run: |
          # فك الضغط
          unzip -o world.zip
          
          # حذف الملف المضغوط (اختياري)
          # rm world.zip
          
          echo "Extraction completed!"
          ls -la
      
      - name: Commit and Push Changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # إضافة المجلد المفكوك
          git add world/
          
          # التحقق من وجود تغييرات
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🗺️ Extract world folder from world.zip"
            git push
            echo "World folder pushed successfully!"
          fi
